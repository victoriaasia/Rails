-#@current_locale = #{@current_locale.inspect}

%button.button__left.theme
  = link_to t('.select_theme_bt'), choose_theme_path, :title => "Select theme from your device folder", locale: @current_locale, :format => "js", remote: true
